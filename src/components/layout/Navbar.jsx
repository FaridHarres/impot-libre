import Header from './Header';

// Navbar is provided as an alias for Header.
// The mobile navigation is integrated directly into the Header component.
export default function Navbar() {
  return <Header />;
}
